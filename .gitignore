# Backup files from migration / cleanup passes
*.bak
*.bak-*

# Large unused source files (kept locally but never deployed)
SPL-POOL-30.jpg
SPL-DEST-07.jpg
CAR-ACC-SUI-99.jpg
IMG_7864.JPG

# CLIENT-CONFIDENTIAL — never commit
proposals/

# Old backups + experimental landing pages
logos-original-backup/
Landing Pages/

# Local source image folders (curated copies go into /cruise-images, /property-images, etc.)
LBB - IMAGE GALLERY/

# Large cruise partner source drops — curated copies go into /cruise-images/<partner>/
cruise-images/_source-zips/
cruise-images/*.zip
cruise-images/*.mov
cruise-images/EXPLORA II - Mediterranean 4.jpg
# Raw brand asset drops in partner folders (Ritz EV_*/LU_*, brand logo .eps, etc.)
cruise-images/**/EV_*
cruise-images/**/LU_*
cruise-images/**/RCYC_*
cruise-images/**/*.eps

# OS noise
.DS_Store
Thumbs.db
*.swp
*~

# Local server log
wbw-server.log
/tmp/wbw-server.log

# Editor metadata
.vscode/
.idea/

# Claude Code session / local settings
.claude/
